# Cloud sync (optional). Leave unset to run in guest-only mode — the app
# works fully offline with no account and no backend.
#
# Both values come from your Supabase project settings (Project Settings ->
# API). Apply supabase/migrations/*.sql to that project first.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
